p() {
    if [[  -n "$1" && "$1" == "cd" ]]; then
        cd $(project-manager cd "$2" --projects-root $PROJECT_HOME);
    else
        project-manager "$@" --projects-root $PROJECT_HOME;
    fi
}

p "$@"
